# Server Configuration
PORT=5000

# MongoDB Configuration
MONGO_URI=mongodb://localhost:27017/credit-scoring

# JWT Authentication (REQUIRED)
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# Node Environment
NODE_ENV=development

# Optional: MongoDB Atlas (if using cloud)
# MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/credit-scoring?retryWrites=true&w=majority

# Cloudinary (Optional - for future profile picture uploads)
# Cloud_Name=your_cloud_name
# Cloud_Api=your_api_key
# Cloud_Secret=your_api_secret

# Email Configuration (Optional - for future email verification)
# MY_GMAIL=your_email@gmail.com
# MY_PASS=your_app_password
